import React, { Fragment } from 'react';
import { TodoForm } from '../TodoForm/TodoForm';

const Tasks = () => {
  return (
    <Fragment>
      <TodoForm />
    </Fragment>
  );
};

export { Tasks };
